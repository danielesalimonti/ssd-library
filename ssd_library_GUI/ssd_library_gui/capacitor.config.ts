import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ssd_library_gui',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
